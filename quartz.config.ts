import { defineConfig } from "@jackyzha0/quartz"

export default defineConfig({
  title: "French Text Project",
  baseUrl: "/",
  author: "caedmon5",
  description: "Quartz site for collaborative publication",
})
